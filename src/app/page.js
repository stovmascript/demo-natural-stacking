import StackingButton from "@/app/components/StackingButton";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      <blockquote>
        The ADP interface is down, back up the online capacitor so we can reboot
        the RSS interface!
      </blockquote>
      <StackingButton />
      <blockquote>
        If we transmit the transmitter, we can get to the RSS monitor through
        the back-end PNG program!
      </blockquote>
    </main>
  );
}
